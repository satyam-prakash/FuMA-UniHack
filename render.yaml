services:
  - type: web
    name: fuma-industrial-engine
    env: python
    region: oregon
    plan: free
    buildCommand: ./build.sh
    startCommand: uvicorn member3.backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
